export enum AccessibilityImpact {
  Critical = "critical",
  Serious = "serious"
}

export enum ColorMode {
  Dark = "dark",
  Light = "light"
}

export enum InputModality {
  Keyboard = "keyboard",
  Pointer = "pointer",
  Touch = "touch"
}

export enum ScenarioActionType {
  Blur = "blur",
  Click = "click",
  Fill = "fill",
  Focus = "focus",
  Press = "press",
  Select = "select"
}

export enum ScenarioSelectorKind {
  Label = "label",
  NodeId = "node-id",
  Role = "role",
  TestId = "test-id",
  Text = "text"
}

export enum ScenarioVersion {
  Version1 = "1.0.0"
}
