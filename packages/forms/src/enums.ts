export enum BuiltInValidatorId {
  NumberConstraints = "number-constraints",
  Required = "required"
}

export enum ValidationErrorCode {
  NumberMaximum = "number-maximum",
  NumberMinimum = "number-minimum",
  NumberStep = "number-step",
  Required = "required"
}

export enum ValidationMessageKey {
  NumberMaximum = "validation.number.maximum",
  NumberMinimum = "validation.number.minimum",
  NumberStep = "validation.number.step",
  Required = "validation.required"
}
