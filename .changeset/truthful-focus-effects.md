---
"@unislang/unifold-renderer-dom": minor
"@unislang/unifold": minor
---

Return an enum-backed focus settlement from the DOM renderer and publish completion only after the
deepest composed active element proves focus was acquired. Wait for accepted pending definitions,
respect user focus movement, and map missing, hidden, disabled, or native-refused targets to the
canonical failed-effect stream outcome.
