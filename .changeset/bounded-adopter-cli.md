---
"@unislang/unifold-cli": minor
---

Add the public-facade document validator and deterministic hierarchy-oriented vanilla starter
generator with safe no-overwrite behavior and generated Playwright coverage. Add bounded local
UiModule project validation plus deterministic flattened runtime and integrity-lock artifacts.
