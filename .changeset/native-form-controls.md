---
"@unislang/unifold-elements": minor
"@unislang/unifold": minor
---

Add native form-associated behavior for scalar text and scalar choice elements, including `FormData`
projection, browser reset/restore and disabled-fieldset callbacks, IME de-duplication, reflected names,
native validity, and enum-backed canonical input origins without adding a second state authority.
