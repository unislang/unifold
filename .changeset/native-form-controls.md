---
"@unislang/unifold-elements": minor
"@unislang/unifold": minor
---

Add generic native form-associated behavior for scalar, boolean, repeated-string, and file controls,
including native `FormData` semantics, browser reset/restore and disabled-fieldset callbacks, IME
de-duplication, reflected names, native validity, strict value-shape codecs, and enum-backed canonical
input origins without adding a second state authority or exposing file data in canonical events.
