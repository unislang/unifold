---
"@unislang/unifold-export": minor
"@unislang/unifold-semantics": minor
"@unislang/unifold": minor
---

Add deterministic native static HTML export for the full reference component set, stable hydration
markers, script-safe Schema.org JSON-LD, classification-aware value omission, and exact artifact
integrity manifests. Safely upgrade exported fallback DOM by preflighting and atomically adopting its
single semantic publication, with complete DOM, value, focus, and metadata rollback on rejection.
