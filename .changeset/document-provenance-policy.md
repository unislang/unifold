---
"@unislang/unifold": minor
---

Add fail-closed document provenance policies with trusted issuer and revocation resolution, exact
payload SHA-256 fingerprints, metadata-only load audits, validated durable receipts, and browser
parity evidence for bindings, validation outcomes, and canonical event semantics.
