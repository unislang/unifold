---
"@unislang/unifold-control-plane": minor
---

Add the versioned, replaceable control-plane server seam and its tenant-isolated in-memory
conformance adapter for authorized revisions, idempotent effects, realtime gap recovery, redacted
audit metadata, integrity-verified backup restore, and exact-resource collaboration authorization.
Add bounded standard-Fetch host/client adapters with exact safe request validation and status
mapping, plus a contiguous resumable realtime cursor that never advances on gaps or malformed data.
