---
"@unislang/unifold-catalog": minor
"@unislang/unifold-elements": minor
---

Add enum-backed reviewed component definitions and schema-validated Custom Elements Manifest output
for all eighteen core elements. Publish generated authoring, attribute, snapshot, and control schemas;
include validation configuration in canonical snapshots; and enforce manifest, catalog, sidecar,
semantic-contract, and executable-evidence drift checks.
